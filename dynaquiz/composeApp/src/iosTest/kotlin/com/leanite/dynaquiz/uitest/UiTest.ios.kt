package com.leanite.dynaquiz.uitest

/**
 * iOS [UiTest] base. Compose UI tests run natively via Skiko on the iOS simulator target
 * (e.g. `iosSimulatorArm64Test`), so no platform runtime adapter is needed here.
 */
actual abstract class UiTest actual constructor()
